# Hosted/Shared Deployment Configuration
# Use these values when deploying to a shared hosting environment

# Limit Monte Carlo to 100 iterations for resource constraints
MAX_MONTE_CARLO_ITERATIONS=100
DEFAULT_MONTE_CARLO_ITERATIONS=50

# Add your production domain(s)
CORS_ORIGINS=https://your-domain.com,http://localhost:5173
