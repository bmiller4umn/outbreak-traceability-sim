# Outbreak Traceability Simulation - Environment Configuration
# Copy this file to .env and adjust values as needed

# =============================================================================
# Monte Carlo Simulation Limits
# =============================================================================

# Maximum number of Monte Carlo iterations allowed (default: 10000)
# Set to a lower value (e.g., 100) for hosted/shared deployments to limit resource usage
MAX_MONTE_CARLO_ITERATIONS=10000

# Default number of iterations when starting a new Monte Carlo simulation (default: 1000)
DEFAULT_MONTE_CARLO_ITERATIONS=1000

# =============================================================================
# CORS Configuration
# =============================================================================

# Comma-separated list of allowed origins for CORS
# Add your production domain here when deploying
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
